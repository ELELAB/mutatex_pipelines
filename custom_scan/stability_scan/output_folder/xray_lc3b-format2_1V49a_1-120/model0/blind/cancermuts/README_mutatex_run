# This directory contains a custom scanning run performed with mutatex. This directory was generated by the following Snakemake pipeline:

# /data/user/morlandi/mutatex_custom/mutatex_pipelines/custom_scan/stability_scan

# The input PDB file has been copied from:

# ../../../input_pdb/lc3b.pdb

# In order to run it, the following steps are required:
# source virtual environment
. /usr/local/envs/mutatex/bin/activate

# A symbolic link is also required to avoid problems with files having multiple dots in the filename
ln -s ../../../input_pdb/lc3b.pdb output_folder/xray_lc3b-format2_1V49a_1-120/model0/blind/cancermuts/lc3b-format2_1V49.pdb

# mutatex command
mutatex lc3b-format2_1V49.pdb -q poslist.txt  -m mutation_list.txt --foldx-version=suite5 --np 1 -x /usr/local/foldx5_2021/foldx -C deep -v -L -c -l >& log &
